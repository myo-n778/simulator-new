# 物理シミュレーター 変更規則

更新日: 2026-08-24

## 優先順位

1. `current-spec.md` の現行仕様
2. `data/simulators.json` のカタログ情報
3. 各シミュレーターの実装
4. 過去メモと途中案

## 変更時の原則

- 正常動作している既存41本は、依頼のない限り変更しない。
- 新規追加は一意な `id` と実在する `route` を持たせる。
- 共通の見た目・操作・描画処理は `assets/physics-lab.css` と `assets/physics-lab.js` に集約する。
- 物理量は内部でSI単位を基準にし、表示時だけ教材向け単位へ変換する。
- 近似値、理想化、損失の扱いはページ内の式または観察の焦点に示す。
- 公開、削除、既存ルート変更は別の明示許可がある場合だけ行う。

## 衝突時の扱い

- 既存ページ固有のUIと共通UIが異なる場合、既存ページは維持する。
- カタログ件数と実在ルートが食い違う場合、実在確認後にカタログを直す。
- 計算式とアニメーションが食い違う場合、式と数値の正しさを先に確定し、その結果に描画を合わせる。
